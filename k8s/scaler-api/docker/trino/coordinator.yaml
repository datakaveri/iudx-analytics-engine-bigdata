apiVersion: apps/v1
kind: Deployment
metadata:
  name: trino-coordinator
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trino-coordinator
  template:
    metadata:
      labels:
        app: trino-coordinator
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: trino-coordinator
          image: "trinodb/trino:latest"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: coordinator
          volumeMounts:
            - name: trino-cfg-vol
              mountPath: /etc/trino/jvm.config
              subPath: jvm.config
            - name: trino-cfg-vol
              mountPath: /etc/trino/config.properties
              subPath: config.properties.coordinator
            - name: trino-cfg-vol
              mountPath: /etc/trino/node.properties
              subPath: node.properties
            - name: trino-cfg-vol
              mountPath: /etc/trino/catalog/kudu.properties
              subPath: kudu.properties
      volumes:
        - name: trino-cfg-vol
          configMap:
            name: trino-configs
